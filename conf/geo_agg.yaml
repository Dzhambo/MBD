defaults:
  - _self_
  - dataset_unsupervised/parquet_geo
  - seq_encoder/geo_agg_seq_encoder
  - inference/inference_agg_geo
  - downstream/downstream

seed_everything: 42
logger_name: geo_agg
model_path: ../models/${logger_name}.p
embed_file_name: agg_geo

pl_module:
    seq_encoder: ${seq_encoder/seq_encoder}